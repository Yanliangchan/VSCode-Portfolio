# GitHub username shown on the /github page's live profile/repos and
# contribution calendar. Public value, but still not hardcoded in source
# so the code stays reusable.
NEXT_PUBLIC_GITHUB_USERNAME=
